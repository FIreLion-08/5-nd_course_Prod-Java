module.exports = {
    entry: './HW-09.js', // Входной файл, где пишем свой код
    output: {
        filename: 'main.js', // Выходной файл, который подключаем к HTML
        // Сохранится он по пути "./dist/main.js"
    },
}
